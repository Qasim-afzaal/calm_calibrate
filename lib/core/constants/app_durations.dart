/// Animation timings — calm, unhurried (desk recovery niche).
abstract final class AppDurations {
  static const fast = Duration(milliseconds: 200);
  static const normal = Duration(milliseconds: 350);
  static const slow = Duration(milliseconds: 500);
  static const page = Duration(milliseconds: 400);
  static const breathe = Duration(milliseconds: 2800);
  static const float = Duration(milliseconds: 3200);
  static const staggerStep = Duration(milliseconds: 80);
}
